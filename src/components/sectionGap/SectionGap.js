import React from 'react'

function SectionGap({ color, width, height }) {
    return (
        <div style={{backgroundColor: color, width: width, height: height}} class="sectionGap">
            
        </div>
    )
}

export default SectionGap
