import * as THREE from 'three'
const state = {
    activeMesh: {},
    activeMeshName: "playlistCard",
    cameraPos: new THREE.Vector3(0,0,4),
    target: new THREE.Vector3(0,0,0),
    shouldUpdate: true
}

export default state